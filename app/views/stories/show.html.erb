
<h3><%= @story.title %></h3>
<% if @story.images.any? %>
  <ul class="list-unstyled">
    <% @sentences.each do |sentence| %>
      <li><%= sentence.text %></li>
    <% end %>
  </ul>
<% else %>
  <p>Add to this tale!!</p>
<% end %>


<%= link_to "Find yourself", story_images_path(@story) %>
<hr>

<%= render 'images/form' %>
